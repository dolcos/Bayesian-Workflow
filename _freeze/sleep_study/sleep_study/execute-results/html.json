{
  "hash": "f284cf10c7158c2cf1a5274d9c602dab",
  "result": {
    "engine": "knitr",
    "markdown": "---\nimage: ../social-cards/sleep_study.png\ntitle: \"Prior specification for regression models: Reanalysis of a sleep study\"\nauthor: \"Paul Bürkner and Aki Vehtari\"\ndate: 2025-10-22\ndate-modified: today\ndate-format: iso\nformat:\n  html:\n    number-sections: true\n    code-copy: true\n    code-download: true\n    code-tools: true\nbibliography: ../casestudies.bib\n---\n\nThis notebook includes the code for the Bayesian Workflow book\nChapter 17 *Prior specification for regression models: Reanalysis\nof a sleep study*.\n\n# Introduction\n\nPrior distributions are at the heart of Bayesian statistics and are\nmentioned as one of its defining features in almost all\nintroductions. Yet, in practice, specifying priors remains a highly\nchallenging and complex topic that tends to cause a lot of\nconfusion for people having to deal with it. In this case study, we\nclarify some of this confusion by explaining the different purposes\nof priors and things that should be considered when specifying\nthem.\n\n\n\n\n\n**Load packages**\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(rprojroot)\nroot <- has_file(\".Bayesian-Workflow-root\")$make_fix_file()\nlibrary(ggplot2)\nlibrary(bayesplot)\n# devtools::load_all(\"~/proj/bayesplot\")\ntheme_set(bayesplot::theme_default(base_family = \"sans\"))\nlibrary(patchwork)\nlibrary(dplyr)\nlibrary(loo)\nlibrary(brms)\noptions(mc.cores = 4)\ndir.create(root(\"sleep_study\",\"models/\"), showWarnings = FALSE)\nBRMS_MODEL_DIR <- root(\"sleep_study\", \"models/\")\noptions(future.globals.maxSize = 1e9)\nlibrary(priorsense)\noptions(priorsense.plot_help_text = FALSE)\n```\n:::\n\n\n# The sleep study data\n\nWe analyze the `sleepstudy` data set\n[@Belenky-Wesensten-Thorne-etal:2003] that is shipped with the R\npackage `lme4` [@Bates-Maechler-Bolker-etal:2015]. The dataset\ncovers 18 people undergoing sleep deprivation (less than 3 hours of\nsleep per night) for 7 consecutive nights, with their average\nreaction times in milliseconds in a simple experiment.\n\nReasons for choosing the `sleepstudy` data set:\n\n- Few variables all of which are easy to understand\n- easy yet important multilevel structure \n- sensible to express with both linear and generalized linear models\n- non-trivial error distributions\n- independent priors are sensible(ish) due to the small number of parameters\n- well known to a lot of R users\n\nDays 0-1 were adaptation and training (T1/T2), day 2 was baseline (B);\nsleep deprivation started after day 2. We Drop days 0-1, and make the\nbaseline to be new 0.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndata(\"sleepstudy\", package = \"lme4\")\nconditions <- make_conditions(sleepstudy, \"Subject\", incl_vars = FALSE)\nsleepstudy <- sleepstudy |>\n  filter(Days >= 2) |>\n  mutate(Days = Days - 2)\n```\n:::\n\n\nPlot the data\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsleepstudy |>\n  ggplot(aes(Days, Reaction)) + \n  geom_point() +\n  facet_wrap(\"Subject\", ncol = 6) +\n  scale_x_continuous(breaks = 0:7) +\n  labs(y = \"Reaction time (ms)\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-data-1.png){#fig-sleepstudy-data width=95%}\n:::\n:::\n\n\n# Simple linear model\n\nPrior base\n\n\n::: {.cell}\n\n```{.r .cell-code}\nprior_lin_base <- prior(normal(200, 100), class = b, coef = \"Intercept\") +\n  prior(normal(0, 20), class = b, coef = \"Days\") +\n  prior(exponential(0.02), class = sigma)\n```\n:::\n\n\n\nModel base and sample from the posterior\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfit_lin_base <- brm(\n  Reaction ~ 0 + Intercept + Days, \n  data = sleepstudy,\n  family = gaussian(),\n  prior = prior_lin_base, \n  file = paste0(BRMS_MODEL_DIR, \"fit_lin_base\"),\n  file_refit = \"on_change\"\n) \n```\n:::\n\n\nPosterior summary\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsummary(fit_lin_base, priors = TRUE)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n Family: gaussian \n  Links: mu = identity \nFormula: Reaction ~ 0 + Intercept + Days \n   Data: sleepstudy (Number of observations: 144) \n  Draws: 4 chains, each with iter = 2000; warmup = 1000; thin = 1;\n         total post-warmup draws = 4000\n\nPriors:\nb_Days ~ normal(0, 20)\nb_Intercept ~ normal(200, 100)\n<lower=0> sigma ~ exponential(0.02)\n\nRegression Coefficients:\n          Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nIntercept   268.06      7.66   253.03   283.67 1.00     1916     1874\nDays         11.39      1.85     7.64    14.91 1.00     1923     1991\n\nFurther Distributional Parameters:\n      Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nsigma    51.14      3.08    45.49    57.70 1.00     2037     1780\n\nDraws were sampled using sample(hmc). For each parameter, Bulk_ESS\nand Tail_ESS are effective sample size measures, and Rhat is the potential\nscale reduction factor on split chains (at convergence, Rhat = 1).\n```\n\n\n:::\n:::\n\n\nPosterior conditional effects\n\n\n::: {.cell}\n\n```{.r .cell-code}\nplot(conditional_effects(fit_lin_base), points = TRUE, plot = FALSE)[[1]] +\n  labs(y = \"Reaction time (ms)\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-conditional_effects-fit_lin_base-1.png){#fig-sleepstudy-conditional_effects-fit_lin_base width=95%}\n:::\n:::\n\n\n# Simple linear model (centered predictors)\n\nPoints to discuss:\n\n- priors on original or centered intercept?\n- dependency of the prior on marginal moments of the data?\n- different qualitative options for priors on b and sigma\n\nPrior 1\n\n\n::: {.cell}\n\n```{.r .cell-code}\nprior1 <- prior(normal(250, 100), class = Intercept) +\n  prior(normal(0, 20), class = b) +\n  prior(exponential(0.02), class = sigma)\n```\n:::\n\n\nSample from the prior 1\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfit1_prior <- brm(\n  Reaction ~ 1 + Days, \n  data = sleepstudy,\n  family = gaussian(),\n  prior = prior1, \n  sample_prior = \"only\",\n  file = paste0(BRMS_MODEL_DIR, \"fit1_prior\"),\n  file_refit = \"on_change\"\n) \n```\n:::\n\n\nPrior predictive checking\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(652312)\npp_check(fit1_prior, ndraws = 100) + \n  ylim(c(0, 0.02)) +\n  theme_sub_axis_y(line = element_blank())\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-pp_check-fit1_prior-1.png){#fig-sleepstudy-pp_check-fit1_prior width=95%}\n:::\n:::\n\n\nModel 1: sample from the posterior\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfit1 <- brm(\n  Reaction ~ 1 + Days, \n  data = sleepstudy,\n  family = gaussian(),\n  prior = prior1, \n  file = paste0(BRMS_MODEL_DIR, \"fit1\"),\n  file_refit = \"on_change\"\n) \n```\n:::\n\n\nPosterior summary\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsummary(fit1, priors = TRUE)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n Family: gaussian \n  Links: mu = identity \nFormula: Reaction ~ 1 + Days \n   Data: sleepstudy (Number of observations: 144) \n  Draws: 4 chains, each with iter = 2000; warmup = 1000; thin = 1;\n         total post-warmup draws = 4000\n\nPriors:\nb ~ normal(0, 20)\nIntercept ~ normal(250, 100)\n<lower=0> sigma ~ exponential(0.02)\n\nRegression Coefficients:\n          Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nIntercept   268.19      7.71   253.00   283.65 1.00     3647     3006\nDays         11.36      1.85     7.65    14.96 1.00     3751     2961\n\nFurther Distributional Parameters:\n      Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nsigma    51.10      3.08    45.64    57.76 1.00     4252     2934\n\nDraws were sampled using sample(hmc). For each parameter, Bulk_ESS\nand Tail_ESS are effective sample size measures, and Rhat is the potential\nscale reduction factor on split chains (at convergence, Rhat = 1).\n```\n\n\n:::\n:::\n\n\nPosterior conditional effects\n\n\n::: {.cell}\n\n```{.r .cell-code}\nplot(conditional_effects(fit1), points = TRUE, plot = FALSE)[[1]] +\n  labs(y = \"Reaction time (ms)\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-conditional_effects-fit1-1.png){#fig-sleepstudy-conditional_effects-fit1 width=95%}\n:::\n:::\n\n\nPosterior predictive checking\n\n\n::: {.cell}\n\n```{.r .cell-code}\npp_check(fit1, ndraws = 50) +\n  theme_sub_axis_y(line = element_blank())\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-pp_check-fit1-1.png){#fig-sleepstudy-pp_check-fit1 width=95%}\n:::\n:::\n\n\nPrior sensitivity analysis\n\n\n::: {.cell}\n\n```{.r .cell-code}\npowerscale_plot_dens(fit1, variable = c(\"b_Intercept\", \"b_Days\", \"sigma\"),\n                     component = \"prior\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-priorsense-fit1-1.png){#fig-sleepstudy-priorsense-fit1 width=95%}\n:::\n:::\n\n\n# Simple linear model (informative priors)\n\nPoints to discuss:\n\n- Priors will be influencing the posterior if chosen to be informative enough\n- For models that are simple relative to the amount of data, prior distributions\n  are unlikely to affect the posterior strongly, unless prior are very informative\n\nPrior 2\n\n\n::: {.cell}\n\n```{.r .cell-code}\nprior2 <- prior(normal(250, 100), class = Intercept) +\n  prior(normal(0, 1), class = b) +\n  prior(exponential(0.02), class = sigma)\n```\n:::\n\n\nModel 2: sample from the posterior\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfit2 <- brm(\n  Reaction ~ 1 + Days, \n  data = sleepstudy,\n  family = gaussian(),\n  prior = prior2, \n  file = paste0(BRMS_MODEL_DIR, \"fit2\"),\n  file_refit = \"on_change\"\n) \n```\n:::\n\n\nPosterior summary\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsummary(fit2)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n Family: gaussian \n  Links: mu = identity \nFormula: Reaction ~ 1 + Days \n   Data: sleepstudy (Number of observations: 144) \n  Draws: 4 chains, each with iter = 2000; warmup = 1000; thin = 1;\n         total post-warmup draws = 4000\n\nRegression Coefficients:\n          Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nIntercept   299.98      5.57   289.52   311.32 1.00     3449     2550\nDays          2.30      0.92     0.50     4.09 1.00     3936     3189\n\nFurther Distributional Parameters:\n      Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nsigma    55.12      3.46    48.87    62.48 1.00     3716     3092\n\nDraws were sampled using sample(hmc). For each parameter, Bulk_ESS\nand Tail_ESS are effective sample size measures, and Rhat is the potential\nscale reduction factor on split chains (at convergence, Rhat = 1).\n```\n\n\n:::\n:::\n\n\nPosterior conditional effects\n\n\n::: {.cell}\n\n```{.r .cell-code}\nplot(conditional_effects(fit2), points = TRUE, plot = FALSE)[[1]] +\n  labs(y = \"Reaction time (ms)\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-conditional_effects-fit2-1.png){#fig-sleepstudy-conditional_effects-fit2 width=95%}\n:::\n:::\n\n\nPrior sensitivity analysis\n\n\n::: {.cell}\n\n```{.r .cell-code}\npowerscale_plot_dens(fit2, variable = c(\"b_Intercept\", \"b_Days\", \"sigma\"),\n                     component = \"prior\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-priorsense-fit2-1.png){#fig-sleepstudy-priorsense-fit2 width=95%}\n:::\n:::\n\n\n# Simple linear model (informative priors with fat tails)\n\nPoints to discuss:\n- tails of the priors (normal vs. student-t)\n\nPrior 2b\n\n\n::: {.cell}\n\n```{.r .cell-code}\nprior2b <- prior(normal(250, 100), class = Intercept) +\n  prior(student_t(7, 0, 1), class = b) +\n  prior(exponential(0.02), class = sigma)\n```\n:::\n\n\nModel 2b: sample from the posterior\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfit2b <- brm(\n  Reaction ~ 1 + Days, \n  data = sleepstudy,\n  family = gaussian(),\n  prior = prior2b, \n  file = paste0(BRMS_MODEL_DIR, \"fit2b\"),\n  file_refit = \"on_change\"\n) \n```\n:::\n\n\nPosterior summary\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsummary(fit2b)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n Family: gaussian \n  Links: mu = identity \nFormula: Reaction ~ 1 + Days \n   Data: sleepstudy (Number of observations: 144) \n  Draws: 4 chains, each with iter = 2000; warmup = 1000; thin = 1;\n         total post-warmup draws = 4000\n\nRegression Coefficients:\n          Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nIntercept   279.36      9.52   261.30   298.55 1.00     3179     2900\nDays          8.14      2.39     3.15    12.60 1.00     3131     2816\n\nFurther Distributional Parameters:\n      Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nsigma    51.72      3.18    46.07    58.46 1.00     3276     2860\n\nDraws were sampled using sample(hmc). For each parameter, Bulk_ESS\nand Tail_ESS are effective sample size measures, and Rhat is the potential\nscale reduction factor on split chains (at convergence, Rhat = 1).\n```\n\n\n:::\n:::\n\n\nPosterior conditional effects\n\n\n::: {.cell}\n\n```{.r .cell-code}\nplot(conditional_effects(fit2b), points = TRUE, plot = FALSE)[[1]] +\n  labs(y = \"Reaction time (ms)\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-conditional_effects-fit2b-1.png){#fig-sleepstudy-conditional_effects-fit2b width=95%}\n:::\n:::\n\n\nPrior sensitivity analysis\n\n\n::: {.cell}\n\n```{.r .cell-code}\npowerscale_plot_dens(fit2b, variable = c(\"b_Intercept\", \"b_Days\", \"sigma\"),\n                     component = \"prior\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-priorsense-fit2b-1.png){#fig-sleepstudy-priorsense-fit2b width=95%}\n:::\n:::\n\n\nIllustrate difference between normal and Student-t prior:\n\n\n::: {.cell}\n\n```{.r .cell-code}\nx <- seq(-4, 4, 0.01)\nd1 <- dnorm(x, 0, 1)\nd2 <- dstudent_t(x, 7, 0, 1)\ndata.frame(\n  d = c(d1, d2), \n  x = rep(x, 2), \n  Prior = rep(c(\"normal(0, 1)\", \"Student-t(7, 0, 1)\"), each = length(x))\n) |>\n  ggplot(aes(x, d, color = Prior)) +\n  geom_line(size = 0.8) +\n  xlab(expression(b[1])) +\n  ylab(\"Density\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-compare-normal-student-density-1.png){#fig-compare-normal-student-density width=95%}\n:::\n:::\n\n\nCompute CI-bound for an exponential prior:\n\n\n::: {.cell}\n\n```{.r .cell-code}\nqexp(c(0.025, 0.975), 0.02)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1]   1.26589 184.44397\n```\n\n\n:::\n:::\n\n\n# Linear varying intercept model\n\nPoints to discuss:\n\n- How to represent unidimensional multilevel structures via priors\n- priors on hyperparameters (SDs)\n- shall the prior on sigma change now that we add more terms?\n\nPrior 3\n\n\n::: {.cell}\n\n```{.r .cell-code}\nprior3 <- prior(normal(250, 100), class = Intercept) +\n  prior(normal(0, 20), class = b) +\n  prior(exponential(0.02), class = sigma) +\n  prior(exponential(0.02), class = sd)\n```\n:::\n\n\nModel 3: sample from the posterior\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfit3 <- brm(\n  Reaction ~ 1 + Days + (1 | Subject), \n  data = sleepstudy,\n  family = gaussian(),\n  prior = prior3, \n  file = paste0(BRMS_MODEL_DIR, \"fit3\"),\n  file_refit = \"on_change\"\n) \n```\n:::\n\n\nPosterior summary\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsummary(fit3)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n Family: gaussian \n  Links: mu = identity \nFormula: Reaction ~ 1 + Days + (1 | Subject) \n   Data: sleepstudy (Number of observations: 144) \n  Draws: 4 chains, each with iter = 2000; warmup = 1000; thin = 1;\n         total post-warmup draws = 4000\n\nMultilevel Hyperparameters:\n~Subject (Number of levels: 18) \n              Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nsd(Intercept)    43.69      8.26    30.63    63.26 1.00      763     1071\n\nRegression Coefficients:\n          Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nIntercept   267.96     11.28   246.10   289.92 1.01      611     1224\nDays         11.40      1.09     9.30    13.57 1.00     3950     2589\n\nFurther Distributional Parameters:\n      Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nsigma    30.44      1.97    26.89    34.55 1.00     3511     2485\n\nDraws were sampled using sample(hmc). For each parameter, Bulk_ESS\nand Tail_ESS are effective sample size measures, and Rhat is the potential\nscale reduction factor on split chains (at convergence, Rhat = 1).\n```\n\n\n:::\n:::\n\n\nPosterior conditional effects\n\n\n::: {.cell}\n\n```{.r .cell-code}\nplot(conditional_effects(fit3), plot = FALSE)[[1]] +\n  labs(y = \"Reaction time (ms)\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-conditional_effects-1-fit3-1.png){#fig-sleepstudy-conditional_effects-1-fit3 width=95%}\n:::\n:::\n\n\nPosterior conditional effects\n\n\n::: {.cell}\n\n```{.r .cell-code}\nplot(conditional_effects(fit3, conditions = conditions, re_formula = NULL),\n     ncol = 6, points = TRUE, plot = FALSE)[[1]] +\n  labs(y = \"Reaction time (ms)\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-conditional_effects-2-fit3-1.png){#fig-sleepstudy-conditional_effects-2-fit3 width=95%}\n:::\n:::\n\n\n# Linear varying intercept and slope model\n\nPoints to discuss:\n\n- How to represent multidimensional multilevel structures via priors\n- priors on hyperparameters (SDs and correlations)\n- The implications of the LKJ prior for correlation matrices\n\nPrior 4\n\n\n::: {.cell}\n\n```{.r .cell-code}\nprior4 <- prior(normal(250, 100), class = Intercept) +\n  prior(normal(0, 20), class = b) +\n  prior(exponential(0.04), class = sigma) +\n  prior(exponential(0.04), class = sd, group = Subject, coef = Intercept) +\n  prior(\"exponential(1.0/15)\", class = sd, group = Subject, coef = Days) +\n  prior(lkj(1), class = cor)\n```\n:::\n\n\nModel 4: sample from the posterior\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfit4 <- brm(\n  Reaction ~ 1 + Days + (1 + Days | Subject), \n  data = sleepstudy,\n  family = gaussian(),\n  prior = prior4, \n  save_pars = save_pars(all = TRUE)\n) \n```\n:::\n\n\nPosterior summary\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsummary(fit4)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n Family: gaussian \n  Links: mu = identity \nFormula: Reaction ~ 1 + Days + (1 + Days | Subject) \n   Data: sleepstudy (Number of observations: 144) \n  Draws: 4 chains, each with iter = 2000; warmup = 1000; thin = 1;\n         total post-warmup draws = 4000\n\nMultilevel Hyperparameters:\n~Subject (Number of levels: 18) \n                    Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nsd(Intercept)          32.04      7.45    19.47    48.62 1.00     2001     2772\nsd(Days)                7.23      1.79     4.22    11.25 1.00     1630     2058\ncor(Intercept,Days)     0.21      0.31    -0.40     0.79 1.00     1102     1562\n\nRegression Coefficients:\n          Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nIntercept   267.51      9.02   248.97   285.35 1.00     1674     2250\nDays         11.26      1.98     7.30    15.17 1.00     1581     2053\n\nFurther Distributional Parameters:\n      Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nsigma    25.91      1.83    22.61    29.80 1.00     3031     2428\n\nDraws were sampled using sampling(NUTS). For each parameter, Bulk_ESS\nand Tail_ESS are effective sample size measures, and Rhat is the potential\nscale reduction factor on split chains (at convergence, Rhat = 1).\n```\n\n\n:::\n:::\n\n\nPosterior conditional effects\n\n\n::: {.cell}\n\n```{.r .cell-code}\nplot(conditional_effects(fit4), plot = FALSE)[[1]] +\n  labs(y = \"Reaction time (ms)\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-conditional_effects-1-fit4-1.png){#fig-sleepstudy-conditional_effects-1-fit4 width=95%}\n:::\n:::\n\n\nPosterior predictive checking\n\n\n::: {.cell}\n\n```{.r .cell-code}\npp_check(fit4) +\n  theme_sub_axis_y(line = element_blank())\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-pp_check-fit4-1.png){#fig-sleepstudy-pp_check-fit4 width=95%}\n:::\n:::\n\n\nPosterior conditional effects\n\n\n::: {.cell}\n\n```{.r .cell-code}\nplot(conditional_effects(fit4, conditions = conditions, re_formula = NULL),\n     ncol = 6, points = TRUE, plot = FALSE)[[1]] +\n  scale_x_continuous(breaks = 0:9) +\n  labs(y = \"Reaction time (ms)\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-conditional_effects-2-fit4-1.png){#fig-sleepstudy-conditional_effects-2-fit4 width=95%}\n:::\n:::\n\n\nIllustrate the marginal LKJ(1) prior for different dimensions.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndmLKJ <- function(x, eta, d) {\n  dbeta((x + 1) / 2, eta + (d - 2)/2, eta + (d - 2)/2)\n}\ndata.frame(x = rep(seq(-0.999, 0.999, 0.001), 3)) |>\n  mutate(\n    d = rep(c(2, 5, 10), each = n()/3),\n    dens = dmLKJ(x, 1, d),\n    d = factor(d)\n  ) |> \n  ggplot(aes(x, dens, color = d)) +\n  geom_line(size = 0.8) +\n  scale_color_viridis_d() +\n  ylab(\"Density\") +\n  xlab(expression(rho))\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-LKJ1-1.png){#fig-sleepstudy-LKJ1 width=95%}\n:::\n:::\n\n\n## Log-Linear prior-only model\n\nReuse priors from the normal linear model: Prior ln1\n\n\n::: {.cell}\n\n```{.r .cell-code}\nprior_ln1 <- prior(normal(250, 100), class = Intercept) +\n  prior(normal(0, 20), class = b) +\n  prior(exponential(0.02), class = sigma)\n```\n:::\n\n\nSample from the prior ln1\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfit_ln1_prior <- brm(\n  Reaction ~ 1 + Days, \n  data = sleepstudy,\n  family = lognormal(),\n  prior = prior_ln1, \n  sample_prior = \"only\",\n  file = paste0(BRMS_MODEL_DIR, \"fit_ln1_prior\"),\n  file_refit = \"on_change\"\n) \n```\n:::\n\n\nPrior predictive checking\n\n\n::: {.cell}\n\n```{.r .cell-code}\npp_check(fit_ln1_prior) +\n  theme_sub_axis_y(line = element_blank())\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-pp_check-fit_ln1_prior-1.png){#fig-sleepstudy-pp_check-fit_ln1_prior width=95%}\n:::\n:::\n\n\nPrior predictive checking\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(652312)\nprp_ln <- apply(posterior_predict(fit_ln1_prior), 2, function(x) mean(x[is.finite(x)]))\nprp_ln_dat <- data.frame(y = sleepstudy$Reaction, yrep = prp_ln)\ngg_ln1_prior <- ggplot(prp_ln_dat, aes(y, yrep)) +\n  geom_point() +\n  scale_x_continuous(trans = \"log10\") +\n  scale_y_continuous(trans = \"log10\") +\n  ylab(\"Mean yrep\")\n```\n:::\n\n\nUse more sensible priors: Prior ln2\n\n\n::: {.cell}\n\n```{.r .cell-code}\nprior_ln2 <- prior(normal(5, 0.55), class = Intercept) +\n  prior(normal(0, 0.2), class = b) +\n  prior(exponential(3), class = sigma)\n```\n:::\n\n\nSample from the prior ln2\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfit_ln2_prior <- brm(\n  Reaction ~ 1 + Days, \n  data = sleepstudy,\n  family = lognormal(),\n  prior = prior_ln2, \n  sample_prior = \"only\",\n  file = paste0(BRMS_MODEL_DIR, \"fit_ln2_prior\"),\n  file_refit = \"on_change\"\n) \n```\n:::\n\n\nPrior predictive checking\n\n\n::: {.cell}\n\n```{.r .cell-code}\npp_check(fit_ln2_prior) +\n  theme_sub_axis_y(line = element_blank())\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-pp_check-fit_ln2_prior-1.png){#fig-sleepstudy-pp_check-fit_ln2_prior width=95%}\n:::\n:::\n\n\nPrior predictive checking\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(652312)\nprp_ln <- apply(posterior_predict(fit_ln2_prior), 2, function(x) mean(x[is.finite(x)]))\nprp_ln_dat <- data.frame(y = sleepstudy$Reaction, yrep = prp_ln)\ngg_ln2_prior <- ggplot(prp_ln_dat, aes(y, yrep)) +\n  geom_point() +\n  scale_x_continuous(trans = \"log10\") +\n  scale_y_continuous(trans = \"log10\") +\n  ylab(\"Mean yrep\")\n```\n:::\n\n\nPrior predictive checking comparing priors ln1 and ln2\n\n\n::: {.cell}\n\n```{.r .cell-code}\ngg_ln1_prior + gg_ln2_prior\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-pp_check-fit_ln1_ln2_prior-1.png){#fig-sleepstudy-pp_check-fit_ln1_ln2_prior width=95%}\n:::\n:::\n\n\nSample from the posterior\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfit_ln2 <- brm(\n  Reaction ~ 1 + Days, \n  data = sleepstudy,\n  family = lognormal(),\n  prior = prior_ln2, \n  file = paste0(BRMS_MODEL_DIR, \"fit_ln2\"),\n  file_refit = \"on_change\"\n) \n```\n:::\n\n\nPosterior summary\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsummary(fit_ln2)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n Family: lognormal \n  Links: mu = identity \nFormula: Reaction ~ 1 + Days \n   Data: sleepstudy (Number of observations: 144) \n  Draws: 4 chains, each with iter = 2000; warmup = 1000; thin = 1;\n         total post-warmup draws = 4000\n\nRegression Coefficients:\n          Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nIntercept     5.59      0.03     5.54     5.64 1.00     4104     2952\nDays          0.04      0.01     0.02     0.05 1.00     4427     2910\n\nFurther Distributional Parameters:\n      Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nsigma     0.17      0.01     0.15     0.19 1.00     3509     2900\n\nDraws were sampled using sampling(NUTS). For each parameter, Bulk_ESS\nand Tail_ESS are effective sample size measures, and Rhat is the potential\nscale reduction factor on split chains (at convergence, Rhat = 1).\n```\n\n\n:::\n:::\n\n\nPosterior conditional effects\n\n\n::: {.cell}\n\n```{.r .cell-code}\nplot(conditional_effects(fit_ln2), plot = FALSE)[[1]] +\n  labs(y = \"Reaction time (ms)\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-conditional_effects-1-ln2-1.png){#fig-sleepstudy-conditional_effects-1-ln2 width=95%}\n:::\n:::\n\n\nPrior sensitivity analysis\n\n\n::: {.cell}\n\n```{.r .cell-code}\npowerscale_plot_dens(fit_ln2, variable = c(\"b_Intercept\", \"b_Days\", \"sigma\"),\n                     component = \"prior\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-priorsense-fit_ln2-1.png){#fig-sleepstudy-priorsense-fit_ln2 width=95%}\n:::\n:::\n\n\n# Log-linear varying intercept and slope model\n\nPoints to discuss:\n\n- positive only family may be preferred theoretically but may not always\n  be required\n- How a non-identity link (log in this case) messes with our intuition\n  about parameters and hence with prior specification\n- how Jensen's inequality makes direct translations of prior difficult\n- how Jacobian adjustment would be needed to ensure equivalence of two\n  prior on different scales.\nPrior 5\n\n\n::: {.cell}\n\n```{.r .cell-code}\nprior5 <- prior(normal(5, 0.55), class = Intercept) +\n  prior(normal(0, 0.2), class = b) +\n  prior(exponential(6), class = sigma) +\n  prior(exponential(6), class = sd, group = Subject, coef = Intercept) +\n  prior(exponential(10), class = sd, group = Subject, coef = Days) +\n  prior(lkj(1), class = cor)\n```\n:::\n\n\nModel5: sample from the posterior\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfit5 <- brm(\n  Reaction ~ 1 + Days + (1 + Days | Subject), \n  data = sleepstudy,\n  family = lognormal(),\n  prior = prior5, \n  file = paste0(BRMS_MODEL_DIR, \"fit5\"),\n  file_refit = \"on_change\"\n) \n```\n:::\n\n\nPosterior summary\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsummary(fit5)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n Family: lognormal \n  Links: mu = identity \nFormula: Reaction ~ 1 + Days + (1 + Days | Subject) \n   Data: sleepstudy (Number of observations: 144) \n  Draws: 4 chains, each with iter = 2000; warmup = 1000; thin = 1;\n         total post-warmup draws = 4000\n\nMultilevel Hyperparameters:\n~Subject (Number of levels: 18) \n                    Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nsd(Intercept)           0.13      0.03     0.09     0.19 1.00     1588     2260\nsd(Days)                0.02      0.01     0.01     0.04 1.00     1800     2348\ncor(Intercept,Days)     0.05      0.28    -0.50     0.59 1.00     1384     1895\n\nRegression Coefficients:\n          Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nIntercept     5.59      0.03     5.52     5.65 1.00      924     1815\nDays          0.04      0.01     0.02     0.05 1.00     1883     2191\n\nFurther Distributional Parameters:\n      Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nsigma     0.08      0.01     0.07     0.09 1.00     2986     3008\n\nDraws were sampled using sampling(NUTS). For each parameter, Bulk_ESS\nand Tail_ESS are effective sample size measures, and Rhat is the potential\nscale reduction factor on split chains (at convergence, Rhat = 1).\n```\n\n\n:::\n:::\n\n\nPosterior conditional effects\n\n\n::: {.cell}\n\n```{.r .cell-code}\nplot(conditional_effects(fit5), plot = FALSE)[[1]] +\n  labs(y = \"Reaction time (ms)\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-conditional_effects-fit5-1.png){#fig-sleepstudy-conditional_effects-fit5 width=95%}\n:::\n:::\n\n\nPosterior predictive checking\n\n\n::: {.cell}\n\n```{.r .cell-code}\npp_check(fit5) +\n  theme_sub_axis_y(line = element_blank())\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-pp_check-fit5-1.png){#fig-sleepstudy-pp_check-fit5 width=95%}\n:::\n:::\n\n\nPosterior predictive checking comparing fit4 and fit5\n\n\n::: {.cell}\n\n```{.r .cell-code}\npp_check(fit4, type = \"intervals\") + labs(y = \"Reaction time (ms)\") +\n  pp_check(fit5, type = \"intervals\") + \n  plot_layout(guides = \"collect\") \n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-pp_check-fit4-fit5-1.png){#fig-sleepstudy-pp_check-fit4-fit5 width=95%}\n:::\n:::\n\n\nPosterior conditional effects\n\n\n::: {.cell}\n\n```{.r .cell-code}\nplot(conditional_effects(fit5, conditions = conditions, re_formula = NULL),\n     ncol = 6, points = TRUE, plot = FALSE)[[1]] +\n  scale_x_continuous(breaks = 0:9) +\n  labs(y = \"Reaction time (ms)\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-conditional_effects-plus-fit5-1.png){#fig-sleepstudy-conditional_effects-plus-fit5 width=95%}\n:::\n:::\n\n\nPrior sensitivity analysis\n\n\n::: {.cell}\n\n```{.r .cell-code}\nvars5 <- c(\"b_Intercept\", \"b_Days\", \"sd_Subject__Intercept\", \n           \"sd_Subject__Days\", \"cor_Subject__Intercept__Days\", \"sigma\")\npowerscale_plot_dens(fit5, variable = vars5, component = \"prior\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-priorsense-fit5-1.png){#fig-sleepstudy-priorsense-fit5 width=95%}\n:::\n:::\n\n\nCompare with linear multilevel model which indicated the lognormal model\nhaving a little better predictive performance.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nloo(fit4, fit5)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nOutput of model 'fit4':\n\nComputed from 4000 by 144 log-likelihood matrix.\n\n         Estimate   SE\nelpd_loo   -692.0 21.6\np_loo        31.4  8.7\nlooic      1384.0 43.2\n------\nMCSE of elpd_loo is NA.\nMCSE and ESS estimates assume MCMC draws (r_eff in [0.5, 1.3]).\n\nPareto k diagnostic values:\n                         Count Pct.    Min. ESS\n(-Inf, 0.7]   (good)     139   96.5%   743     \n   (0.7, 1]   (bad)        5    3.5%   <NA>    \n   (1, Inf)   (very bad)   0    0.0%   <NA>    \nSee help('pareto-k-diagnostic') for details.\n\nOutput of model 'fit5':\n\nComputed from 4000 by 144 log-likelihood matrix.\n\n         Estimate   SE\nelpd_loo   -681.1 19.2\np_loo        32.3  8.0\nlooic      1362.2 38.3\n------\nMCSE of elpd_loo is NA.\nMCSE and ESS estimates assume MCMC draws (r_eff in [0.5, 1.3]).\n\nPareto k diagnostic values:\n                         Count Pct.    Min. ESS\n(-Inf, 0.7]   (good)     139   96.5%   280     \n   (0.7, 1]   (bad)        4    2.8%   <NA>    \n   (1, Inf)   (very bad)   1    0.7%   <NA>    \nSee help('pareto-k-diagnostic') for details.\n\nModel comparisons:\n model elpd_diff se_diff p_worse diag_diff      diag_elpd\n  fit5       0.0     0.0      NA           5 k_psis > 0.7\n  fit4     -10.9     4.4    0.99           5 k_psis > 0.7\n```\n\n\n:::\n:::\n\n\n# log-linear distributional multilevel model\nPoints to discuss:\n\n- Parameters for standard deviations on the log or log-log scale\n  or hard to understand and hence set priors on.\n- This is specifically true for standard deviations of standard deviations\n  on the log or log-log scale.\n- In theory, we would not need the\n  exponential link on sigma but then we had to care for the positivity of the\n  varying intercepts on sigma and hence would have to specify, for example, an\n  hierarchical Gamma rather than a hierarchical normal prior.\n- Look at prior predictions for the correlations to demonstrate\n  the effect of the LKJ prior for larger than 2x2 matrices\n\nPrior 6\n\n\n::: {.cell}\n\n```{.r .cell-code}\nprior6 <- prior(normal(5.5, 0.55), class = Intercept) +\n  prior(normal(0, 0.2), class = b) +\n  prior(normal(0, 0.3), class = Intercept, dpar = sigma) +\n  prior(exponential(3), class = sd, group = Subject, coef = Intercept) +\n  prior(exponential(5), class = sd, group = Subject, coef = Days) +\n  prior(exponential(3), class = sd, dpar = sigma, group = Subject) +\n  prior(lkj(1), class = cor)\n```\n:::\n\n\nModel 6: sample from the posterior\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfit6 <- brm(\n  bf(Reaction ~ 1 + Days + (1 + Days |S| Subject),\n     sigma ~ 1 + (1 |S| Subject)),\n  data = sleepstudy,\n  family = lognormal(),\n  prior = prior6, \n  file = paste0(BRMS_MODEL_DIR, \"fit6\"),\n  file_refit = \"on_change\"\n) \n```\n:::\n\n\nPosterior summary\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsummary(fit6)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n Family: lognormal \n  Links: mu = identity; sigma = log \nFormula: Reaction ~ 1 + Days + (1 + Days | S | Subject) \n         sigma ~ 1 + (1 | S | Subject)\n   Data: sleepstudy (Number of observations: 144) \n  Draws: 4 chains, each with iter = 2000; warmup = 1000; thin = 1;\n         total post-warmup draws = 4000\n\nMultilevel Hyperparameters:\n~Subject (Number of levels: 18) \n                               Estimate Est.Error l-95% CI u-95% CI Rhat\nsd(Intercept)                      0.16      0.04     0.10     0.25 1.00\nsd(Days)                           0.03      0.01     0.02     0.05 1.00\nsd(sigma_Intercept)                1.87      0.40     1.13     2.74 1.01\ncor(Intercept,Days)               -0.01      0.28    -0.54     0.54 1.00\ncor(Intercept,sigma_Intercept)     0.25      0.40    -0.59     0.84 1.00\ncor(Days,sigma_Intercept)          0.25      0.40    -0.60     0.87 1.00\n                               Bulk_ESS Tail_ESS\nsd(Intercept)                       999     1724\nsd(Days)                            987     1144\nsd(sigma_Intercept)                1082     1812\ncor(Intercept,Days)                 976     1436\ncor(Intercept,sigma_Intercept)      334      667\ncor(Days,sigma_Intercept)           422      753\n\nRegression Coefficients:\n                Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nIntercept           5.63      0.09     5.47     5.81 1.01      335      695\nsigma_Intercept    -0.95      0.37    -1.71    -0.25 1.00     1668     2117\nDays                0.04      0.02     0.01     0.08 1.00      416      929\n\nDraws were sampled using sampling(NUTS). For each parameter, Bulk_ESS\nand Tail_ESS are effective sample size measures, and Rhat is the potential\nscale reduction factor on split chains (at convergence, Rhat = 1).\n```\n\n\n:::\n:::\n\n\nPosterior conditional effects\n\n\n::: {.cell}\n\n```{.r .cell-code}\nplot(conditional_effects(fit6, conditions = conditions, re_formula = NULL),\n     ncol = 6, points = TRUE, plot = FALSE)[[1]] +\n  labs(y = \"Reaction time (ms)\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-conditional_effects-fit6-1.png){#fig-sleepstudy-conditional_effects-fit6 width=95%}\n:::\n:::\n\n\n# Exgaussian distributional multilevel model\n\nPoints to discuss:\n\n- All the models before are relatively robust to the choice of priors:\n  Even completely flat priors work ok and don't produce much different results\n  because the models are parsimonious relative to the amount of data and quality\n- This robustness is no longer the case for the upcoming models\n- Exgaussian is linear yet can handle strong right skewness at the expense\n  of not strictly respecting the lower boundary of zero\n- Avoids the log-log awkwardness of the lognormal and related models\n- Interpretation of the beta (skewness) parameter at least somewhat\n  intuitive\n\nPrior 7\n\n\n::: {.cell}\n\n```{.r .cell-code}\nprior7 <- prior(normal(250, 100), class = Intercept) +\n  prior(normal(0, 20), class = b) +\n  prior(normal(0, 5), class = Intercept, dpar = sigma) +\n  prior(normal(0, 5), class = beta) +\n  prior(exponential(0.04), class = sd, group = Subject, coef = Intercept) +\n  prior(exponential(0.05), class = sd, group = Subject, coef = Days) +\n  prior(exponential(0.2), class = sd, dpar = sigma, group = Subject) +\n  prior(lkj(1), class = cor)\n```\n:::\n\n\nModel 7: sample from the posterior\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfit7 <- brm(\n  bf(Reaction ~ 1 + Days + (1 + Days |S| Subject),\n     sigma ~ 1 + (1 |S| Subject)), \n  data = sleepstudy,\n  family = exgaussian(),\n  prior = prior7, \n  inits = 0,\n  cores = 4,\n  file = paste0(BRMS_MODEL_DIR, \"fit7\"),\n  file_refit = \"on_change\"\n) \n```\n:::\n\n\nPosterior summary for model 7\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsummary(fit7)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n Family: exgaussian \n  Links: mu = identity; sigma = log \nFormula: Reaction ~ 1 + Days + (1 + Days | S | Subject) \n         sigma ~ 1 + (1 | S | Subject)\n   Data: sleepstudy (Number of observations: 144) \n  Draws: 4 chains, each with iter = 2000; warmup = 1000; thin = 1;\n         total post-warmup draws = 4000\n\nMultilevel Hyperparameters:\n~Subject (Number of levels: 18) \n                               Estimate Est.Error l-95% CI u-95% CI Rhat\nsd(Intercept)                     77.15     61.08     1.42   169.78 3.46\nsd(Days)                          18.65     12.66     1.20    37.63 4.40\nsd(sigma_Intercept)                1.56      1.27     0.38     3.58 3.78\ncor(Intercept,Days)                0.76      0.26     0.31     0.97 3.14\ncor(Intercept,sigma_Intercept)     0.13      0.61    -0.75     0.90 2.93\ncor(Days,sigma_Intercept)          0.26      0.75    -0.97     0.91 3.40\n                               Bulk_ESS Tail_ESS\nsd(Intercept)                         4       11\nsd(Days)                              4       11\nsd(sigma_Intercept)                   4       12\ncor(Intercept,Days)                   5       18\ncor(Intercept,sigma_Intercept)        5       24\ncor(Days,sigma_Intercept)             4       14\n\nRegression Coefficients:\n                Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nIntercept          55.15     69.75    -2.80   168.44 3.27        4       13\nsigma_Intercept     2.10      2.45    -1.00     5.57 3.05        5       46\nDays               -5.01      6.05   -12.34     1.15 3.01        5       27\n\nFurther Distributional Parameters:\n     Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nbeta    22.80     31.07     1.53    78.90 3.49        4       12\n\nDraws were sampled using sampling(NUTS). For each parameter, Bulk_ESS\nand Tail_ESS are effective sample size measures, and Rhat is the potential\nscale reduction factor on split chains (at convergence, Rhat = 1).\n```\n\n\n:::\n:::\n\n\nPosterior conditional effects\n\n\n::: {.cell}\n\n```{.r .cell-code}\nplot(conditional_effects(fit7, conditions = conditions, re_formula = NULL),\n     ncol = 6, points = TRUE, plot = FALSE)[[1]] +\n  labs(y = \"Reaction time (ms)\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-conditional_effects-fit7-1.png){#fig-sleepstudy-conditional_effects-fit7 width=95%}\n:::\n:::\n\n\n# Exgaussian distributional multilevel model (default priors)\n\nPoints to discuss:\n\n- some parameters are better informed by the data than others\n- using our custom priors and default priors barely matters for most\n  except for the skewness parameter where the effect is clearly visible.\n- default priors are already chosen in an effort to be sensible(ish)\n\nModel 8: sample from the posterior\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfit8 <- brm(\n  bf(Reaction ~ 1 + Days + (1 + Days |S| Subject),\n     sigma ~ 1 + (1 |S| Subject)), \n  data = sleepstudy,\n  family = exgaussian(),\n  # no prior argument: default priors are used\n  inits = 0,\n  cores = 4,\n  file = paste0(BRMS_MODEL_DIR, \"fit8\"),\n  file_refit = \"on_change\"\n) \n```\n:::\n\n\nPosterior summary\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsummary(fit8)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n Family: exgaussian \n  Links: mu = identity; sigma = log \nFormula: Reaction ~ 1 + Days + (1 + Days | S | Subject) \n         sigma ~ 1 + (1 | S | Subject)\n   Data: sleepstudy (Number of observations: 144) \n  Draws: 4 chains, each with iter = 2000; warmup = 1000; thin = 1;\n         total post-warmup draws = 4000\n\nMultilevel Hyperparameters:\n~Subject (Number of levels: 18) \n                               Estimate Est.Error l-95% CI u-95% CI Rhat\nsd(Intercept)                     68.00     62.45    27.06   177.37 3.66\nsd(Days)                           9.08      1.93     6.14    11.57 3.87\nsd(sigma_Intercept)                2.88      3.49     0.75     9.04 3.16\ncor(Intercept,Days)                0.25      0.39    -0.36     0.76 3.74\ncor(Intercept,sigma_Intercept)     0.20      0.58    -0.77     0.74 3.48\ncor(Days,sigma_Intercept)          0.19      0.45    -0.52     0.71 3.63\n                               Bulk_ESS Tail_ESS\nsd(Intercept)                         4       11\nsd(Days)                              4       12\nsd(sigma_Intercept)                   4       12\ncor(Intercept,Days)                   4       11\ncor(Intercept,sigma_Intercept)        4       14\ncor(Days,sigma_Intercept)             4       11\n\nRegression Coefficients:\n                Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nIntercept         196.52    115.46    -3.65   267.03 4.00        4       11\nsigma_Intercept     1.45      2.23    -2.41     3.08 3.33        4       13\nDays                8.45      4.91     0.14    12.32 4.13        4       11\n\nFurther Distributional Parameters:\n     Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nbeta    24.16     39.18     1.36   103.26 4.20        4       11\n\nDraws were sampled using sampling(NUTS). For each parameter, Bulk_ESS\nand Tail_ESS are effective sample size measures, and Rhat is the potential\nscale reduction factor on split chains (at convergence, Rhat = 1).\n```\n\n\n:::\n:::\n\n\nPosterior conditional effects\n\n\n::: {.cell}\n\n```{.r .cell-code}\nplot(conditional_effects(fit8, conditions = conditions, re_formula = NULL),\n     ncol = 6, points = TRUE, plot = FALSE)[[1]] +\n  labs(y = \"Reaction time (ms)\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-conditional_effects-fit8-1.png){#fig-sleepstudy-conditional_effects-fit8 width=95%}\n:::\n:::\n\n\n# Exgaussian distributional multilevel model (flat priors)\n\nPoints to discuss:\n\n- without reasonable(ish) priors, sampling may fall apart with some\n  really long-running chains, divergent transitions and increased Rhats.\n- priors matter, but if you don't specify quite informative priors\n  or have small data relative to the model complexity, you will\n  likely not see a difference compared to sensible default priors\n\nExtract all default priors:\n\n\n::: {.cell}\n\n```{.r .cell-code}\nprior9 <- get_prior(\n  bf(Reaction ~ 1 + Days + (1 + Days |S| Subject),\n     sigma ~ 1 + (1 |S| Subject)), \n  data = sleepstudy,\n  family = exgaussian()\n)\n```\n:::\n\n\nMake all priors flat (except for the varying coefficients' priors):\n\n\n::: {.cell}\n\n```{.r .cell-code}\nprior9$prior <- \"\"\n```\n:::\n\n\nModel 9: sample from the posterior\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfit9 <- brm(\n  bf(Reaction ~ 1 + Days + (1 + Days |S| Subject),\n     sigma ~ 1 + (1 |S| Subject)), \n  data = sleepstudy,\n  family = exgaussian(),\n  prior = prior9, \n  inits = 0,\n  cores = 4,\n  file = paste0(BRMS_MODEL_DIR, \"fit9\"),\n  file_refit = \"on_change\"\n) \n```\n:::\n\n\nPosterior summary\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsummary(fit9)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n Family: exgaussian \n  Links: mu = identity; sigma = log \nFormula: Reaction ~ 1 + Days + (1 + Days | S | Subject) \n         sigma ~ 1 + (1 | S | Subject)\n   Data: sleepstudy (Number of observations: 144) \n  Draws: 4 chains, each with iter = 2000; warmup = 1000; thin = 1;\n         total post-warmup draws = 4000\n\nMultilevel Hyperparameters:\n~Subject (Number of levels: 18) \n                               Estimate Est.Error l-95% CI u-95% CI Rhat\nsd(Intercept)                     68.47     64.40    30.28   186.07 3.81\nsd(Days)                           9.05      1.49     7.24    12.44 3.16\nsd(sigma_Intercept)                2.65      3.12     0.65     8.20 3.16\ncor(Intercept,Days)                0.32      0.35    -0.06     0.90 3.44\ncor(Intercept,sigma_Intercept)    -0.06      0.21    -0.40     0.21 4.19\ncor(Days,sigma_Intercept)          0.39      0.35    -0.14     0.80 3.49\n                               Bulk_ESS Tail_ESS\nsd(Intercept)                         4       11\nsd(Days)                              5       23\nsd(sigma_Intercept)                   4       14\ncor(Intercept,Days)                   4       12\ncor(Intercept,sigma_Intercept)        4       15\ncor(Days,sigma_Intercept)             4       15\n\nRegression Coefficients:\n                Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nIntercept         192.83    109.85     2.34   260.22 3.22        4       15\nsigma_Intercept     1.54      1.93    -1.85     2.82 3.67        4       12\nDays                8.86      4.46     1.08    12.10 3.55        4       11\n\nFurther Distributional Parameters:\n     Estimate Est.Error l-95% CI u-95% CI Rhat Bulk_ESS Tail_ESS\nbeta    15.16     22.60     1.52    59.80 3.26        4       11\n\nDraws were sampled using sampling(NUTS). For each parameter, Bulk_ESS\nand Tail_ESS are effective sample size measures, and Rhat is the potential\nscale reduction factor on split chains (at convergence, Rhat = 1).\n```\n\n\n:::\n:::\n\n\nPosterior conditional effects\n\n\n::: {.cell}\n\n```{.r .cell-code}\nplot(conditional_effects(fit9, conditions = conditions, re_formula = NULL),\n     ncol = 6, points = TRUE, plot = FALSE)[[1]] +\n  labs(y = \"Reaction time (ms)\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-conditional_effects-fit9-1.png){#fig-sleepstudy-conditional_effects-fit9 width=95%}\n:::\n:::\n\n\n# Model comparison and checking\n\nCompute LOO-CV for models 3, 4, and 5\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfit3 <- add_criterion(fit3, criterion = \"loo\", save_psis = TRUE, reloo = TRUE)\nfit4 <- add_criterion(fit4, criterion = \"loo\", save_psis = TRUE, reloo = TRUE)\nfit5 <- add_criterion(fit5, criterion = \"loo\", save_psis = TRUE, reloo = TRUE)\n```\n:::\n\n\nCompare varying intercept and slope models with normal and log-normal data models\n\n\n::: {.cell}\n\n```{.r .cell-code}\nloo_compare(fit4, fit5)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n model elpd_diff se_diff p_worse diag_diff diag_elpd\n  fit5       0.0     0.0      NA                    \n  fit4     -11.8     4.9    0.99                    \n```\n\n\n:::\n:::\n\n\nLog-normal is not different from normal model, which makes sense as\nall reaction times are fra from 0.\n\nPosterior predictive checking of fit4 using LOO predictive intervals\n\n\n::: {.cell}\n\n```{.r .cell-code}\npp_check(fit4, type = \"loo_intervals\") +\n  labs(y = \"Reaction time (ms)\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-ppc-loo_intervals-fit4-1.png){#fig-sleepstudy-ppc-loo_intervals-fit4 width=95%}\n:::\n:::\n\n\nThere are clearly some outliers.\n\nCreate varying intercept (fit3t) and varying intercept and slope\n(fit4t) models with Student's $t$ data model\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfit3t <- update(fit3, family = student())\nfit4t <- update(fit4, family = student())\n```\n:::\n\n\nCompute LOO-CV for models 3, 4, and 5\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfit3t <- add_criterion(fit3t, criterion = \"loo\", save_psis = TRUE)\nfit4t <- add_criterion(fit4t, criterion = \"loo\", save_psis = TRUE)\n```\n:::\n\n\nPosterior predictive checking of fit4t using LOO predictive intervals\n\n\n::: {.cell}\n\n```{.r .cell-code}\npp_check(fit4t, type = \"loo_intervals\") +\n  labs(y = \"Reaction time (ms)\")\n```\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-ppc-loo_intervals-fit4t-1.png){#fig-sleepstudy-ppc-loo_intervals-fit4t width=95%}\n:::\n:::\n\n\nThe LOO predictive intervals look better now.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntheme_set(bayesplot::theme_default(base_family = \"sans\", base_size = 14))\npp_check(fit4, type = \"loo_pit_ecdf\", method = \"correlated\", moment_match = TRUE) +\n  pp_check(fit4t, type = \"loo_pit_ecdf\", method = \"correlated\") \n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nSome PIT values larger than 1! Largest:  1 \nRounding PIT > 1 to 1.\n```\n\n\n:::\n\n::: {.cell-output-display}\n![](sleep_study_files/figure-html/fig-sleepstudy-ppc-loo_pit-fit4-fit4t-1.png){#fig-sleepstudy-ppc-loo_pit-fit4-fit4t width=95%}\n:::\n:::\n\n\nLooking at the LOO-PIT plots, we see that normal and log-normal\nmodels have too wide predictive distribution for most observations,\nwhich is due to a few outliers inflating the residual\nscale. LOO-PIT plot for Student's $t$ model looks better.\nCompare normal and Student's $t$ models\n\n\n::: {.cell}\n\n```{.r .cell-code}\nloo_compare(fit4, fit4t)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n model elpd_diff se_diff p_worse diag_diff diag_elpd\n fit4t       0.0     0.0      NA                    \n  fit4     -40.6    13.8    1.00                    \n```\n\n\n:::\n:::\n\n\nStudent's $t$ model has much better predictive performance.\n\nExamine how much adding varying slope improved predictive performance in case of normal data model:\n\n\n::: {.cell}\n\n```{.r .cell-code}\nloo_compare(fit3, fit4)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n model elpd_diff se_diff p_worse diag_diff diag_elpd\n  fit4       0.0     0.0      NA                    \n  fit3     -13.3    10.3    0.90                    \n```\n\n\n:::\n:::\n\n\nExamine how much adding varying slope improved predictive\nperformance in case of Student's $t$ data model:\n\n\n::: {.cell}\n\n```{.r .cell-code}\nloo_compare(fit3t, fit4t)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n model elpd_diff se_diff p_worse diag_diff diag_elpd\n fit4t       0.0     0.0      NA                    \n fit3t     -45.7     8.6    1.00                    \n```\n\n\n:::\n:::\n\n\nWhen using Student's $t$ model, the predictive performance difference\nbetween not using or using varying slope is bigger.\n\n# References {.unnumbered}\n\n::: {#refs}\n:::\n\n# Licenses {.unnumbered}\n\n* Code &copy; 2025, Paul Bürkner and Aki Vehtari, licensed under BSD-3.\n* Text &copy; 2025, Paul Bürkner and Aki Vehtari, licensed under CC-BY-NC 4.0.",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}